# Cbox ID — Build status

Living record of what is implemented, verified, and pending — built in dependency order.
"Verified" means: real tests green + PHPStan max clean + Pint clean. Nothing is marked done
on inspection alone. This file exists so the build is auditable: no hidden gaps.

Legend: ✅ done & verified · 🔨 in progress · ⏳ pending (blocked by a dependency) · ⬜ not started

**Current (2026-07-25): kernels (5/5) and every domain module below are complete and verified** —
**1257 tests green**, PHPStan level max clean, Pint clean, `composer audit` clean.

Per-module counts below are the tests in that module's own directory under `tests/`, and are
**derived, not maintained by hand**. Regenerate them rather than editing a number:

```sh
vendor/bin/pest --log-junit /tmp/junit.xml >/dev/null
php -r '$x=new SimpleXMLElement(file_get_contents("/tmp/junit.xml"));$c=[];
  $w=function($n)use(&$w,&$c){foreach($n->testcase as $t){$f=(string)$t["file"];if($f==="")continue;
    $p=explode("/",str_replace(getcwd()."/","",$f));$m=$p[2]??"(root)";$c[$m]=($c[$m]??0)+1;}
    foreach($n->testsuite as $s)$w($s);};
  $w($x);arsort($c);foreach($c as $k=>$v)printf("%-18s %d\n",$k,$v);'
```

The header used to read 1005 — the running total through `Directory` in the table order below,
i.e. a partial sum that was never re-taken after the modules under it landed. Every count in the
tables was re-derived from the command above, not adjusted from that base.

The rows below account for 1088 of the 1257. The remaining 169 are in modules that have no row
here because they are covered by another module's row or are pure app-plane support:
`Platform` 68, `Provisioning` 30, `Otp` 22, `Usage` 22, `AuditStreaming` 11, `Console` 11,
`tests/External` 4, and one root `ConfigTest`. Counts nested inside a row (the 21 SAML HTTP
tests, the 95 SCIM tests) are subsets of that row's own total, not additions to it.

## Kernels
| Module | Status | Notes |
|---|---|---|
| `Kernel\Tenancy` | ✅ | Deny-by-default scope, cross-tenant write guard, runAs/withoutScope, **scopedTo(set) roll-up** for hierarchy. Ships `GenericTenant` + `Testing\InteractsWithTenancy`. 23 tests (the platform-wide `isolation` group is 26). |
| `Kernel\Crypto` | ✅ | KeyManager (RS256/ES256, JWKS, rotation), TokenSigner (firebase/php-jwt, forced alg-allowlist — rejects `alg=none` / RS↔HS confusion / forgery / expiry), SecretBox (XChaCha20-Poly1305 AEAD envelope, context-bound). 31 tests. composer audit clean. |
| `Kernel\Audit` | ✅ | Append-only, per-scope hash chain (`SHA256(canonical(entry) ‖ prev_hash)`), deny-nothing tamper/deletion detection via `verifyChain`, Crypto-signed `checkpoint`. Ships `FakeAuditLog` + `InteractsWithAudit`. 22 tests incl. tamper + deletion detection. |
| `Kernel\Events` | ✅ | Transactional outbox: `emit` persists in the caller's tx (no dual-write, proven by a rollback test), durable `flushPending` relay dispatches `EventDelivered` at-least-once, idempotent. Ships `FakeEventBus` + `InteractsWithEvents`. 14 tests. |
| `Kernel\Authorization` | ✅ | Entitlements (billing-fed projection: set/revoke/reconcile, versioned+history, event+audit) · owned **ReBAC** tuple engine (direct + bounded recursive userset expansion, cycle-safe) · **PDP** (deny-by-default) delegating to both. Ships `InteractsWithEntitlements` + `InteractsWithAuthorization`. 20 tests. |

## Domain
| Module | Status | Notes |
|---|---|---|
| `Organization` | ✅ | Concrete `Tenant` (key = id) · closure-tree hierarchy (arbitrary depth: ancestors/descendants/isDescendantOf/`manages` for reseller transitive access) · memberships (tenant-scoped via `runAs`, dogfoods isolation) · events + audit. Ships `InteractsWithOrganizations`. 86 tests. |
| `Identity` | ✅ | Global users, federated identity links (idempotent provisioning), sessions (start/active/revoke/revoke-all, expiry), argon2-capable password auth. Events + audit. Ships `InteractsWithIdentity`. 128 tests. TOTP MFA ✅ (RFC 6238, verified against the RFC test vectors; secret sealed via Crypto). Passkeys ✅ (credential store + ceremony orchestration + **sign-count clone/replay detection**). **WebAuthn crypto ✅ (real):** `NativeWebAuthnVerifier` verifies registration + assertion signatures via **OpenSSL** (ES256/P-256 + RS256) with **CBOR/COSE decoded by vetted spomky-labs/cbor-php** — no hand-rolled crypto. Enforces ceremony type, challenge binding, origin + RP-id hash, user-presence; supports `none` + self-attestation `packed` (rejects unverifiable x5c chains). Bound only when `rp_id`/`origin` configured, else refuses. Tested against a **real software authenticator** (genuine EC/RSA keypairs, signed assertions) incl. tamper/wrong-challenge/foreign-origin/wrong-RP + end-to-end register→authenticate + clone detection. Magic-link ✅ (single-use hashed token → session, provisions user). |
| `Federation` | ✅ | ✅ Connections (per-org IdP config sealed via Crypto, per-connection routing, active-only resolution) + login orchestration (validated principal → user + membership + session; dogfoods Identity/Organization). **OIDC `AssertionValidator` ✅ (real crypto):** `id_token`/JWS verified via firebase/php-jwt with the key **pinned to RS256** (closes alg-confusion / `none`), `exp`/`nbf` enforced, `iss`/`aud` asserted, JWKS multi-key (`kid`) + single-key; tested against real generated RSA keypairs incl. unknown-key/wrong-iss/wrong-aud/expired/no-sub + end-to-end SSO. **SAML `AssertionValidator` ✅ (real crypto):** wraps `onelogin/php-saml` (XML-DSig verification, XSW defense, XXE-safe parsing) in strict mode with `wantAssertionsSigned`; tested against a genuinely XML-DSig-signed Response (via xmlseclibs + self-signed cert) incl. tamper/unsigned/wrong-audience/untrusted-key. Type-dispatching validator rejects any type with no registered validator (deny-by-default). 72 tests. |
| `Directory` | ✅ | SCIM provisioning core: directory registration (bearer token hashed), provision/update (→ local user via Identity + link + org membership), deactivate/deprovision → **drops membership + revokes sessions immediately**. Events + audit. Ships `InteractsWithDirectory`. 48 tests — the growth is `ScimUserFilter`, which now type-checks a filter literal against its attribute (a boolean attribute compared to `"fasle"` is `400 invalidFilter`, not a silent false) and parses the `meta.lastModified` watermark a delta sync is built on. **The SCIM HTTP endpoint** — PATCH semantics, filter parsing, RFC 7644 interop — lives in the `Api` layer; it is documented in [`docs/core-concepts/scim.md`](docs/core-concepts/scim.md). |
| `OAuthServer` | ✅ | ✅ Clients + service accounts (secrets hashed, **overlap credential rotation**), **client-credentials/M2M** token issuance + introspection + revocation — stateless RS256 JWTs signed & alg-allowlist-verified by the Crypto kernel, `jti`-tracked for revocation. **Authorization-code + PKCE (S256)** ✅: single-use SHA-256-hashed codes, 60s TTL, transactional `lockForUpdate` exchange, exact `redirect_uri`/client match, constant-time PKCE + reuse/expiry rejection (`AuthorizationCodes` contract). **Now shipping:** refresh-token rotation (reuse detection → family revocation), **DPoP** sender-constraining, **Pushed Authorization Requests** (`PushedAuthorizationRequests`), **device grant** (`DeviceAuthorization`), **dynamic client registration** (`DynamicClientRegistration` + RFC 7592 management). Events + audit. Ships `InteractsWithOAuth`. 69 tests. **Host boundary:** the interactive browser **consent / authorize screen is provided by the deployable app** (cbox-id's `/oauth/authorize`), not this package — the package ships the protocol endpoints, tokens and decisions the UI drives. |
| `AccessControl` | ✅ | RBAC: roles + permissions + assignments; hierarchy-aware `can()`/`permissionsFor()` — roles roll DOWN from ancestor orgs (reseller management), never up/sideways. Events + audit. Ships `InteractsWithAccessControl`. 64 tests, plus 4 in `tests/External`. (Entitlements live in the Authorization kernel.) **Bring-your-own RBAC ✅:** `access_control.driver` (`builtin`\|`external`) — under `external` the built-in tables + migrations are gated off (subdir + non-recursive loader) and the contracts fall back deny-by-default (`NullAccessChecker` refuses + stamps empty token claims; `UnboundRoles`/`UnboundGroupRoleMappings` throw `ExternalRbacNotBound`) until the host binds its own adapter, so the platform coexists with an existing Spatie permission install and still feeds its roles into the issued tokens. See `docs/extension-points/custom-rbac.md` (`tests/External`). |
| `AuditQuery` | ✅ | Authorized read surface over the audit trail: filter by action/actor, cursor (sequence) pagination, scope isolation (org vs system), and `since()` pull-stream for SIEM. 6 tests. (Setup reuses the Audit kernel's `InteractsWithAudit`.) |
| `Webhooks` | ✅ | Endpoint registry (secrets sealed via Crypto SecretBox) · HMAC-SHA256 signed HTTP delivery · listens to `EventDelivered` (full Events→webhook fan-out proven end-to-end). Ships `InteractsWithWebhooks`. 46 tests. **Delivery is QUEUED, not synchronous** (0.56.x): the relay dispatches a `DeliverWebhook` job (`ShouldBeUnique`, `$uniqueFor` = `webhooks.stranded_after_seconds`) instead of sending in-band, so one blackholing endpoint no longer holds up another tenant's events, and a delivery whose job was lost is rescued by the retry sweep rather than left `pending` forever. **This makes a running queue worker a hard requirement** — without one, nothing is ever delivered and no error is raised; see [`UPGRADING.md`](UPGRADING.md). Retries are exponential-backoff (`min(60, 2^attempt)` minutes, capped by `webhooks.max_attempts`, then dead-lettered), and each endpoint has a **circuit breaker** (`webhooks.circuit_breaker.failure_threshold` consecutive failures opens it for `cooldown_seconds`; it records on `circuit_opened_at`/`consecutive_failures`, never on `status`, so `paused` stays operator intent). The signing envelope is pinned by a golden fixture shared verbatim with every client SDK. |
| `TokenVault` | ✅ | AI token vault: downstream third-party credentials **sealed** via Crypto `SecretBox` (recoverable, AEAD row-bound — not hashed, the vault must replay them) + **deny-by-default leased access** to agent clients (`SecretVault`: store/rotate/revoke/grant/revokeGrant/lease). Uniform `LeaseDenied` (no enumeration oracle; reason audited only), advisory lease TTL capped per-grant, every op audited without the value, environment-owned. Ships `InteractsWithTokenVault` + `FakeTokenVault`. 32 tests (lifecycle, access deny-by-default, environment isolation, sealed-at-rest + AEAD binding). **Honest scope:** lease TTL advisory; master-key rotation is a manual re-seal (`key_version` column). |
| `OAuthServer` → CIBA | ✅ | **OpenID Connect CIBA (poll mode)** — human-in-the-loop approval for agent actions (`BackchannelAuthentication`): `POST /oauth/backchannel_authentication` + `urn:openid:params:grant-type:ciba` on the token endpoint (returns access + id_token). Device-grant hardening: CSPRNG `auth_req_id` hashed at rest, single-use under `lockForUpdate`, TTL, poll `slow_down`; client polling-secret and host approval-handle are separate identifiers. **Host boundary:** notification + approval UI is the app's; the package emits `oauth.backchannel_authentication_requested`. 12 HTTP tests. |
| `Governance` | ✅ | Access governance (IGA). **Certification campaigns** (`AccessReviews`): snapshot an org's direct role assignments + memberships as items, reviewers certify/revoke, `close()` APPLIES revokes via the real contracts (`Roles::unassign`/`Memberships::remove`); pending-at-close defaults to Revoke (deny-by-default); a refused revoke (last owner) is recorded un-applied + audited (`governance.access.revoke_blocked`), never dropped; overdue campaigns auto-close via `cbox-id:governance:close-overdue` (withoutScope→runAs per env). **Segregation of Duties** (`SegregationOfDuties`): mutually-exclusive role-set policies, a pre-grant `evaluate()`→`Decision` gate + `wouldViolate()`, and `violationsFor`/`scan` detectors. Added additive read methods to `Roles` (`assignmentsForSubject`/`assignmentsInOrganization`). Env-owned, audited (correlated by campaign_id). Ships `InteractsWithGovernance`. 21 tests. **Honest scope:** roles + memberships only (entitlements = billing projection; ReBAC tuples lack enum/audit); direct grants (inherited access governed at the ancestor). |
| `ExternalActions` | ✅ | Inline hooks / external actions: synchronous extension points that ENRICH or VETO an operation (vs webhooks, which only notify). Generic pipeline (`ActionPipeline`) folds in-process actions (deny-by-default config registry) + external HTTP endpoints; first deny short-circuits, enrichment merges. **External HTTP action** (`ExternalActions` + `HttpActionTransport`): SSRF-guarded (reuses cboxdk/laravel-ssrf — assert at register, IP-pin per send, no redirects, TLS on), HMAC-signed (`X-Cbox-Signature`, reveal-once sealed secret), short timeout, NO retry, **fail-closed** (config `fail_open`). **Flagship wiring:** the `TokenMinting` hook in `JwtTokenIssuer` (fires on all 5 grants) — enriches claims (reserved claims protected) or vetoes issuance (`ActionDenied` → `access_denied`, before the jti row is written). Env-owned endpoints, audited. Ships `InteractsWithExternalActions` + `FakeActionTransport`. 25 tests. **Honest scope:** TokenMinting is the only wired hook in v1 (pipeline is generic); the call is on the token hot path (fast, no retry). |
| `SamlIdp` | ✅ | **The platform as a SAML 2.0 Identity Provider** (the inbound direction `Federation` does not cover: here a customer's app is the SP and we assert). Registry of service providers · published IdP metadata with the signing certificate · signed assertions (RSA-SHA256, never SHA-1) that a real SP — `onelogin/php-saml` in strict mode — accepts, with audience, Recipient, `InResponseTo`, NameID and attributes all bound · **redirect and POST bindings**, the redirect signature verified over the octets as transmitted rather than a re-serialization · **single logout** on both bindings. Deny-by-default throughout: an unregistered issuer, a disabled SP, a mismatched ACS, an unsigned request where the SP requires signing, a tampered or XSW-wrapped request, and a signature that does not verify are all refused. 0.56.x additionally enforces `NameIDPolicy` against what the SP can actually be answered under, requires a `Destination` on a signed `AuthnRequest`, and makes an `AuthnRequest` **single-use and <15 min old**; the published EntityID is **frozen at first publication** (`idp_entity_ids`) while the endpoints follow the host, so moving the host never silently re-identifies the IdP. Ships `InteractsWithSamlIdp` + `FakeServiceProviders`. 52 tests, plus 21 more on the HTTP surface in `Api`. See [`docs/core-concepts/saml-idp.md`](docs/core-concepts/saml-idp.md). |
| `Maintenance` | ✅ | **NEW in 0.56.x.** Bounded retention for the tables that grow without limit. `cbox-id:prune` sweeps ten tables (`dpop_proofs`, `consumed_assertions`, `oauth_authorization_codes`, `oauth_access_tokens`, `oauth_refresh_tokens`, `events`, `auth_sessions`, `usage_metered_events`, `webhook_deliveries`, `provisioning_operations`) in chunks, per-table retention from `cbox-id.prune.retention_days.*`, `--dry-run` and `--table=` supported, scheduled daily when `prune.schedule` is on. **`audit_logs` is deliberately NOT prunable** and is not a case of `PrunableTable` at all, so `--table=audit_logs` errors rather than sweeping: the trail is hash-chained and checkpoint-anchored, so deleting below a checkpoint breaks `verifyChain()` and deleting up to one removes the row the tamper check reads — a prune that makes the tamper-evidence report tampering has destroyed the control. Exporting the trail is `src/AuditStreaming/`, not this. Ships alongside `cbox-id:events:backlog` (outbox depth, `--json`, `--fail-over=N`) so relay lag is observable rather than silent. 21 tests. |
| `Api` | ✅ | ✅ Machine endpoints (HTTP-tested): `/.well-known/jwks.json`, OIDC + OAuth AS/protected-resource discovery, **`POST /oauth/token`** (authorization_code+PKCE → access_token + signed id_token; client_credentials), `POST /oauth/introspect`, `POST /oauth/revoke`, `GET|POST /oauth/userinfo`, `POST /oauth/par`, `POST /oauth/device_authorization`, dynamic client registration (`/oauth/register` + RFC 7592 management), `/up`. Per-request environment resolution via `ResolveEnvironment`. 308 tests across the whole HTTP surface. **SAML ACS ✅** (`POST /sso/saml/{connection}/acs`, unauthenticated — the XML signature is the auth): resolves the connection, validates via `AssertionValidator`, drives `FederationFlow` → session; onelogin's Destination/Recipient check is pinned to the connection's configured `sp_acs_url` (proxy-safe, host-independent). 5 HTTP tests incl. genuinely-signed happy path, untrusted-key/unknown/inactive/missing. **SCIM HTTP endpoint ✅ (full RFC 7644 provisioning lifecycle):** bearer-authed `/scim/v2/Users` — filtered **list** (`filter=userName/externalId eq …`, `startIndex`/`count` pagination, ListResponse; unsupported filters → `400 invalidFilter`), create, read, **PATCH** (active + core attributes via path & pathless value-object ops), **PUT** full replace, delete; deprovision revokes sessions end-to-end. 95 tests. See [`docs/core-concepts/scim.md`](docs/core-concepts/scim.md) for the endpoint reference — including the error semantics that changed in 0.56.x (`DELETE` of an unknown id is 404, a PATCH with no `Operations` is 400, a non-boolean `active` is 400, 429 is framed in the SCIM Error envelope, `GET /Groups` omits `members` unless asked, and `userName`/email equality is case-insensitive on every driver). **Host boundary:** the interactive OIDC **authorize / consent UI ships in the deployable app** (cbox-id's `/oauth/authorize`), not this package — as does the general REST CRUD management surface and its published OpenAPI documents (cbox-id's `resources/openapi/{account,environment}.yaml`, served at `/api/v1/openapi.yaml` and `/api/v1/environment/openapi.yaml`). This package ships the protocol endpoints, not a management REST API. |

## DX standard (every module must ship this)
- **Interface-driven** — public behaviour behind `Contracts/` so it is mockable/swappable.
- **Shippable test helpers** in a `Testing/` namespace (Laravel `fake()`-style ergonomics), e.g.
  `InteractsWithTenancy` — consumers testing their own tenant-scoped code get first-class support.
- **Extension points documented** — overridable methods, container bindings, and simple value
  objects (e.g. `GenericTenant`) so the package is easy to extend and adopt piecemeal.
- The package's own tests **dogfood** these helpers, so the DX is proven, not asserted.

## Hierarchy model (arbitrary depth) — where it lives
- Kernel change (done): `scopedTo([keys])` bounded roll-up scope. Everything else is layered on top:
- `Organization`: org tree (`parent_id` + `organization_closure`), `type` (customer/reseller); intra-org
  `org_units` tree (`parent_id` + closure, scoped by `organization_id`); cycle + max-depth guards.
- `AccessControl`: transitive management via closure ancestor checks; entitlement roll-down via
  ancestor walk; management grants. Cross-org action is always authorized `runAs` + audit.

## Tracked integration points (deliberately deferred, not gaps)
These cannot be built before their dependency exists; wiring them is part of the dependent module.

- **Request tenant resolution** — middleware that sets the tenant from the authenticated
  session/token. Lands with `Identity` + `Api`.
- **Queue tenant propagation** — captured tenant key restored in the worker (jobs must not run
  tenant-less or the deny-by-default scope silently returns nothing). Needs a `Tenant` resolvable
  from a key → lands with `Organization`.
- **`withoutScope` auditing** — `Kernel\Audit` now exists. To keep the tenancy kernel dependency-free,
  this is a **call-site convention**: whoever suspends scoping records an `AuditEvent`. Enforced at the
  domain call sites (review + tests), not wired into the kernel.

## Verification commands (per package)
    composer install
    vendor/bin/pint --test        # style
    vendor/bin/phpstan analyse    # static, level max
    vendor/bin/pest               # tests
    vendor/bin/pest --group=isolation   # the load-bearing tenant-isolation proofs
